<script>
	function validarCampo(value, user, pass) {
		if (value === undefined | value === '') {
			const message = `Favor informar ${user} "${pass}"`;
			alert(message);
			throw Error(message);
		}
	}
	valideLogin();
</script>